# 請求内訳書ジェネレーター 要件定義（ドラフト）

参考サンプル: 紙の請求内訳書（個人情報を含むためリポジトリには含めない）

## 1. 目的
出張・学会参加などの請求内訳書（交通費・宿泊費・参加費など）を、ブラウザ上で素早く入力し、端正なPDFとして出力する。

## 2. 基本方針
- **ブラウザだけで完結する静的Webアプリ**（サーバー・DB・ログインなし）
  - ローカル: HTMLファイルを開くだけで動く（研究室の共用PC・自宅PCで利用可）
  - 公開: 同じファイルをGitHub Pages等に置けばURLで利用可（データは送信されない）
- 入力データはブラウザ内に保存し、**JSONエクスポート／インポート**で端末間を移動できる
- UI/UXは「気持ちよく速く入力できる」ことを重視し、出力するPDFは端正な公式書類風にする

## 3. 機能要件

### 3.1 申請者情報
- 番号（学籍番号・職員番号など）と氏名
- 一度入力すれば次回から自動で入る

### 3.2 登録データ（マスタ）★再利用
**初期状態は空**。利用者が自分で登録したものだけが、以降の入力でプルダウン（検索付きコンボボックス）に出る。

#### 地点（中心となるマスタ）
- 駅・停留所・大学・宿泊先など、任意の地点を登録する（例: 十日市場駅）
- 項目: 名称（必須）、補足ラベル（任意。例:「自宅最寄り駅」）
- 交通費の明細行では、出発地・到着地の両方をこの地点リストから選ぶ
- 未登録の名称を入力して欄から離れると自動で登録する（通知の「取り消す」で撤回可）。候補の「＋『十日市場駅』を登録」からも登録できる
- よく使う地点・最近使った地点を候補の上位に表示する

#### 経路の記憶（自動）
- 利用者が明示的に登録しなくても、一度入力した「出発地→到着地」の組み合わせの補足（路線・便名）と運賃を記憶する
- 次回、同じ組み合わせを選ぶと補足と運賃を自動で入れる（上書き可）

#### 宿泊先・その他費目
- 宿泊先: 名称、郵便番号、住所。一度入力すれば次回から選べる
- その他費目: 名称、既定金額（任意）。一度入力すれば次回から選べる

#### 管理
- マスタ一覧の管理画面（追加・編集・削除・並べ替え・統合）を用意する
- マスタもJSONエクスポート／インポートの対象に含める（申請データとは別ファイルでも出せる）

### 3.3 交通費
- 日付ごとのグループに、明細行（出発地→到着地／補足／金額）を並べる
- 行の追加・削除・複製・並べ替え（ドラッグ）
- **往復の反転複製**（A→B を B→A として追加。経由地も逆順になる）
- **経由地**: 「＋ 経由地」で乗換駅などを複数追加できる（登録済み地点から選択）。PDFでは「十日市場駅 →（東京駅 経由）→ 金沢駅」と表示し、区間の記憶にも含める（経由地が異なる場合は運賃を自動入力しない）
- 日付グループの複製（連日同じ経路を使う場合）
- 交通費合計の自動計算

### 3.4 宿泊費
- 宿泊先（マスタから選択、または自由入力）
- チェックイン日・チェックアウト日から「◯泊◯日」を自動表示
- 宿泊費合計（金額入力）
- 複数の宿泊先に対応

### 3.5 その他費目
- 講演会参加費など、名称と金額の行を自由に追加

### 3.6 出力（PDF）
- A4縦。タイトル、右上に番号・氏名、費目ごとの表、各小計
- 総合計欄（要確認）
- 入力画面の横にリアルタイムでプレビューを表示
- 印刷ダイアログ経由でPDF保存（日本語フォントをきれいに出すため）
- 領収書の画像やPDFを末尾ページに添付（任意）

### 3.7 保存
- 入力途中の内容を自動保存（ブラウザのローカルストレージ）
- JSONでの書き出し・読込

## 4. 非機能要件
- インストール不要。最新のChrome／Edgeで動作すること
- 個人情報（氏名・番号・住所）を外部に送信しない
- 入力画面はPC前提。スマホでは閲覧できれば可

## 5. 対象外（初期リリースでは作らない）
- 経路検索サービスと連携した運賃の自動取得
- サーバー側での保存・ユーザー管理

## 6. 決定事項（v0.1 の仮決め）
- 総合計欄: あり（PDF上部に「請求金額合計」）
- デザイン: 白黒印刷でも読めるモノクロ基調
- 様式: 自由書式
- 技術: 単一HTMLファイル（`index.html`）。ビルド不要、外部依存はGoogle Fontsのみ（オフライン時はOS標準フォントになる）

## 7. 今後の候補
- 領収書PDFの添付（現状は画像のみ）
- 区間の並べ替え（ドラッグ）
- GitHub Pages での公開
